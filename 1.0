# AE 文本批量替换 / AE Text Batch Replace

一个 After Effects 脚本（ExtendScript / ScriptUI），用于**批量替换项目里所有文本图层的文字**，适合做多语言本地化、模板文案替换等场景。

核心思路：把所有文本按 `<<<1>>>`、`<<<2>>>` 的编号拼成一大段 → 整段复制出去翻译 → 整段粘贴回来 → 脚本按编号自动写回对应图层。

## 特性

- 支持**整个项目**或**当前合成**两种范围
- 一键导出所有文本，按编号连续排列，方便整段复制
- 整段粘贴译文即可批量写回，无需逐行操作
- 保留字体、颜色、描边、位置等原有样式
- 支持 `Source Text` 有关键帧的图层
- 应用时可一次 `Ctrl+Z` 撤销全部修改
- 纯 ScriptUI，无外部依赖，不联网

## 安装

1. 下载 `文本批量替换.jsx`（或 `TextBatchReplace.jsx`）。
2. 放到 AE 的脚本目录（可选，用于常驻菜单）：
   ```
   Adobe After Effects <版本>/Support Files/Scripts/ScriptUI Panels/
   ```
3. 重启 AE，在 `窗口` 菜单里即可看到面板。
   也可以直接 `文件 > 脚本 > 运行脚本文件` 临时运行。

> 文件请保存为 **UTF-8** 编码，否则中文会乱码。

## 使用方法

1. 打开 AE 工程，顶部下拉框选择范围：`整个项目` 或 `当前合成`。
2. 点 **刷新**，左侧会列出所有文本图层的原文。
3. 点左侧文本框 → `Ctrl+A` → `Ctrl+C`，复制全部内容。
4. 丢给 DeepL / ChatGPT / 翻译工具进行翻译。
   **务必保留 `<<<1>>>`、`<<<2>>>` 这类编号标记。**
5. 把翻译结果整段粘贴到右侧文本框。
6. 点 **应用全部修改**，脚本按编号写回对应图层。

## 文本格式示例

左侧导出：

```
<<<1>>>
Hello World
<<<2>>>
Welcome to our channel
<<<3>>>
Subscribe now
```

右侧粘贴（翻译后，编号保留）：

```
<<<1>>>
你好，世界
<<<2>>>
欢迎来到我们的频道
<<<3>>>
现在订阅
```

## 注意事项

- **编号标记不能丢**。脚本解析不到 `<<<1>>>` 会拒绝执行，不会乱写。
- 若某个图层不想替换，把对应区块内容清空即可。
- 图层若用**表达式**驱动 `Source Text`，替换后会被表达式重新覆盖（AE 机制，脚本无法绕过）。
- 若 `Source Text` 有关键帧，脚本会把所有关键帧文字统一改成译文。
- 翻译工具可能改坏 `<<< >>>` 标记（如变成全角），如遇此问题可自行替换为其它不易被改的标记。
- MOGRT / Essential Graphics 主属性文本也会被处理，但受关键帧 / 表达式影响时表现可能不同。

## 兼容性

- 已在 After Effects CC 及以上版本测试。
- 使用 ExtendScript（ES3）编写，不依赖 Node.js 或现代浏览器 API。

## 已知限制

- 不处理未使用（未被任何合成引用）的合成？—— 当前版本会遍历项目里**所有** `CompItem`。
- 不处理 RTL（阿拉伯语 / 希伯来语）排版。
- 长文本翻译后可能溢出原文本框，需要手动微调。

## License

MIT
